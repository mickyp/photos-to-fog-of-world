---
description: 檔案異動後以繁中提交並同步專案文件（無適用文件時視情況新增）
alwaysApply: true
---

# 提交與文件（繁體中文）

在完成一輪**實質程式或內容異動**、準備回報任務完成之前，必須執行下列事項。

## Git 提交

- 使用 **`git add`** 納入本次相關變更後，執行 **`git commit`**。
- **Commit message 一律使用繁體中文**：簡述「做了什麼」與「為什麼」（必要時一句即可）；避免無意義的單字標題。
- 若僅修正筆試或極小 typo，仍應提交，訊息如：`修正某某檔案錯字`。

## 文件更新（依變更範圍對應）

| 變更範圍 | 優先更新的文件 |
|----------|------------------|
| GPX 編輯器（`scripts/gpx_editor*`） | `GPX_EDITOR.md` |
| 使用者可見行為、版本梗概 | `CHANGELOG.md` 的 `[Unreleased]` |
| 專案導覽、安裝、入口 | `README.md` / `README.zh-TW.md`（擇一或並排更新與變更相關段落） |

- **沒有現成對應文件時**：在 `docs/` 下新增簡短說明檔（繁中），檔名清楚反映主題（例如新子系統名稱），並在 `README.md` 或 `CHANGELOG.md` 加一行連結或條目，避免孤島文件。
- **不要**為每個細碎重排建立長篇新文；以「相關、簡潔、可找到」為準。

## 與 `.cursorrules` 的關係

專案根目錄 `.cursorrules` 中的環境與指令偏好仍適用；本規則補充**提交語言**與**文件對照表**的具體要求。
